class AxyraAuth{constructor(){this.currentUser = null;this.isAuthenticated = false;this.init();}init(){console.log('🔐 Inicializando sistema de autenticación AXYRA...');this.checkAuthStatus();this.setupEventListeners();}checkAuthStatus(){try{const userData = localStorage.getItem('axyra_isolated_user');if(userData){this.currentUser = JSON.parse(userData);this.isAuthenticated = true;console.log('✅ Usuario autenticado con localStorage:',this.currentUser.username);this.updateUI();}else{if(window.axyraFirebase && window.axyraFirebase.auth){this.checkFirebaseAuth();}else{console.log('ℹ️ No hay usuario autenticado');this.redirectToLogin();}}}catch(error){console.error('❌ Error verificando autenticación:',error);this.redirectToLogin();}}async checkFirebaseAuth(){try{const auth = window.axyraFirebase.auth;const user = auth.currentUser;if(user){const userData ={username:user.displayName || user.email || 'Usuario',email:user.email,uid:user.uid,provider:user.providerData[0]?.providerId || 'password',};this.currentUser = userData;this.isAuthenticated = true;localStorage.setItem('axyra_isolated_user',JSON.stringify(userData));localStorage.setItem('axyra_firebase_user',JSON.stringify(user));console.log('✅ Usuario autenticado con Firebase:',userData.username);this.updateUI();}else{console.log('ℹ️ No hay usuario autenticado en Firebase');this.redirectToLogin();}}catch(error){console.error('❌ Error verificando Firebase auth:',error);this.redirectToLogin();}}updateUI(){if(!this.isAuthenticated || !this.currentUser)return;const usernameElements = document.querySelectorAll('[data-username]');usernameElements.forEach((element)=>{element.textContent = this.currentUser.username;});const usernameInput = document.getElementById('usernameInput');if(usernameInput){usernameInput.value = this.currentUser.username;}const emailInput = document.getElementById('emailInput');if(emailInput){emailInput.value = this.currentUser.email || '';}const authElements = document.querySelectorAll('[data-auth]');authElements.forEach((element)=>{const authType = element.getAttribute('data-auth');if(authType === 'authenticated'){element.style.display = this.isAuthenticated ? 'block':'none';}else if(authType === 'unauthenticated'){element.style.display = this.isAuthenticated ? 'none':'block';}});}setupEventListeners(){const updateUsernameBtn = document.querySelector('[onclick*="actualizarUsername"]');if(updateUsernameBtn){updateUsernameBtn.addEventListener('click',(e)=>{e.preventDefault();this.updateUsername();});}const verifyEmailBtn = document.querySelector('[onclick*="enviarVerificacionEmail"]');if(verifyEmailBtn){verifyEmailBtn.addEventListener('click',(e)=>{e.preventDefault();this.verifyEmail();});}const changePasswordBtn = document.querySelector('[onclick*="cambiarContraseña"]');if(changePasswordBtn){changePasswordBtn.addEventListener('click',(e)=>{e.preventDefault();this.changePassword();});}const logoutBtn = document.querySelector('[onclick*="logout"]');if(logoutBtn){logoutBtn.addEventListener('click',(e)=>{e.preventDefault();this.logout();});}}async updateUsername(){try{const usernameInput = document.getElementById('usernameInput');if(!usernameInput){console.error('❌ Campo usernameInput no encontrado');this.showNotification('Error:Campo de nombre de usuario no encontrado','error');return;}const newUsername = usernameInput.value.trim();if(!newUsername){this.showNotification('Por favor ingresa un nombre de usuario válido','error');return;}this.currentUser.username = newUsername;localStorage.setItem('axyra_isolated_user',JSON.stringify(this.currentUser));if(window.axyraFirebase && window.axyraFirebase.auth){const user = window.axyraFirebase.auth.currentUser;if(user){await user.updateProfile({displayName:newUsername,});}}this.updateUI();this.showNotification('Nombre de usuario actualizado correctamente','success');console.log('✅ Usuario actualizado:',this.currentUser);}catch(error){console.error('❌ Error actualizando username:',error);this.showNotification('Error actualizando nombre de usuario:' + error.message,'error');}}async verifyEmail(){try{if(!window.axyraFirebase || !window.axyraFirebase.auth){this.showNotification('Sistema de verificación de email no disponible','warning');return;}const user = window.axyraFirebase.auth.currentUser;if(!user){this.showNotification('No hay usuario autenticado','error');return;}if(user.emailVerified){this.showNotification('El email ya está verificado','info');return;}await user.sendEmailVerification();this.showNotification('Email de verificación enviado. Revisa tu bandeja de entrada.','success');console.log('✅ Email de verificación enviado');}catch(error){console.error('❌ Error verificando email:',error);this.showNotification('Error enviando verificación de email:' + error.message,'error');}}async changePassword(){try{if(!window.axyraFirebase || !window.axyraFirebase.auth){this.showNotification('Sistema de cambio de contraseña no disponible','warning');return;}const newPassword = prompt('Ingresa tu nueva contraseña(mínimo 6 caracteres):');if(!newPassword || newPassword.length < 6){this.showNotification('La contraseña debe tener al menos 6 caracteres','error');return;}const user = window.axyraFirebase.auth.currentUser;if(!user){this.showNotification('No hay usuario autenticado','error');return;}await user.updatePassword(newPassword);this.showNotification('Contraseña actualizada correctamente','success');console.log('✅ Contraseña actualizada');}catch(error){console.error('❌ Error cambiando contraseña:',error);this.showNotification('Error cambiando contraseña:' + error.message,'error');}}logout(){try{localStorage.removeItem('axyra_isolated_user');localStorage.removeItem('axyra_firebase_user');if(window.axyraFirebase && window.axyraFirebase.auth){window.axyraFirebase.auth.signOut();}this.currentUser = null;this.isAuthenticated = false;console.log('✅ Sesión cerrada correctamente');this.redirectToLogin();}catch(error){console.error('❌ Error en logout:',error);this.redirectToLogin();}}redirectToLogin(){const currentPath = window.location.pathname;if(!currentPath.includes('login-optimized.html')&& !currentPath.includes('register.html')){window.location.href = '../../login-optimized.html';}}showNotification(message,type = 'info',duration = 5000){let container = document.getElementById('notificationContainer');if(!container){container = document.createElement('div');container.id = 'notificationContainer';container.style.cssText = ` position:fixed;top:20px;right:20px;z-index:10000;max-width:400px;`;document.body.appendChild(container);}const notification = document.createElement('div');notification.style.cssText = ` background:${this.getNotificationColor(type)};color:white;padding:15px 20px;border-radius:8px;margin-bottom:10px;box-shadow:0 4px 6px rgba(0,0,0,0.1);animation:slideInRight 0.3s ease;`;notification.innerHTML = ` <div style="display:flex;justify-content:space-between;align-items:center;"> <span>${message}</span> <button onclick="this.parentElement.parentElement.remove()" style="background:none;border:none;color:white;cursor:pointer;font-size:18px;">×</button> </div> `;container.appendChild(notification);setTimeout(()=>{if(notification.parentElement){notification.remove();}},duration);}getNotificationColor(type){const colors ={success:'#10b981',error:'#ef4444',warning:'#f59e0b',info:'#3b82f6',};return colors[type] || colors.info;}}document.addEventListener('DOMContentLoaded',()=>{window.axyraAuth = new AxyraAuth();});const authNotificationStyles = document.createElement('style');authNotificationStyles.textContent = ` @keyframes slideInRight{from{transform:translateX(100%);opacity:0;}to{transform:translateX(0);opacity:1;}}`;document.head.appendChild(authNotificationStyles);window.AxyraAuth = AxyraAuth;